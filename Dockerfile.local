# LOCAL-ONLY image: copies the jar already built on the host (warm Maven cache),
# avoiding a slow/flaky in-container Maven download. Profile + config come from compose.
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
